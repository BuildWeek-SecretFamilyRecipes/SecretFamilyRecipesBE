const request = require('supertest');
const server = require('../server.js');

describe('/api/users', () => {
    describe('/:id', () => {
        it('should return user', () => {
            
        })
    })
})